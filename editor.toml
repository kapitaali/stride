# stride configuration
# RIDE-compatible gateway defaults (localhost:4502)

gateway_host = "0.0.0.0"
gateway_port = 4502
#gateway_executable = "../rust-apl/target/debug/apl"
gateway_executable = "/home/theb/Apps/array/kap-jvm-text/bin/kap-jvm-text"
gateway_args = "--ride"
#gateway_args = "--serve"
# Environment variables to set when spawning the gateway
gateway_env = ["RIDE_INIT=CONNECT:localhost:4502"]
apl_version = "GNU APL 2.0 (Rust)"
auto_connect = false
max_results = 500
